####################################################################################
## BACKENDS ########################################################################
####################################################################################

## Default backend with no servers that will return a 503 as it has no servers specified.
backend default_backend
  description [BACKEND] Catch-all backend with no servers specified - will return a 503 error if hit.
